apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parlassets
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - files.parlametar.hr
      secretName: parlassets-tls
  rules:
    - host: files.parlametar.hr
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parlassets
                port:
                  number: 80
