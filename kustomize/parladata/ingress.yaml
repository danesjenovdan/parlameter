apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parladata-bosna
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - data.ba.parlameter.si
      secretName: parladata-bosna-tls
  rules:
    - host: data.ba.parlameter.si
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parladata-bosna
                port:
                  number: 8000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parladata-bosna-nginx
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - files.data.ba.parlameter.si
      secretName: parladata-bosna-nginx-tls
  rules:
    - host: files.data.ba.parlameter.si
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parladata-bosna-nginx
                port:
                  number: 80
