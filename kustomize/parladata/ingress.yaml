apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parladata-hrastnik
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - data.hrastnik.parlameter.si
      secretName: parladata-tls
  rules:
    - host: data.hrastnik.parlameter.si
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parladata-hrastnik
                port:
                  number: 8000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parladata-hrastnik-nginx
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - files.data.hrastnik.parlameter.si
      secretName: parladata-hrastnik-nginx-tls
  rules:
    - host: files.data.hrastnik.parlameter.si
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parladata-hrastnik-nginx
                port:
                  number: 80
