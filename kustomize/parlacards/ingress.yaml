apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parlacards
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - parlacards-slovenija.lb.djnd.si
        - kartica.parlameter.si
      secretName: parlacards-tls
  rules:
    - host: parlacards-slovenija.lb.djnd.si
      http: &http_paths
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parlacards
                port:
                  number: 3000
    - host: kartica.parlameter.si
      http: *http_paths
