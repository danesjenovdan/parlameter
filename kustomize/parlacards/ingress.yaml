apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parlacards
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - glej.ajdovscina.parlameter.si
        - kartica.ajdovscina.parlameter.si
      secretName: parlacards-tls
  rules:
    - host: glej.ajdovscina.parlameter.si
      http: &http_paths
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parlacards
                port:
                  number: 3000
    - host: kartica.ajdovscina.parlameter.si
      http: *http_paths
