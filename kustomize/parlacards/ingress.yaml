apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parlacards
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - gledaj.10.parlametar.hr
        - kartica.parlametar.hr
      secretName: parlacards-tls
  rules:
    - host: gledaj.10.parlametar.hr
      http: &http_paths
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parlacards
                port:
                  number: 3000
    - host: kartica.parlametar.hr
      http: *http_paths
