apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parlasite-xi
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - 11.parlametar.hr
      secretName: parlasite-xi-tls
  rules:
    - host: 11.parlametar.hr
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parlasite-xi
                port:
                  number: 3066
