name: Build and publish Docker image
on:
  push:
    branches:
      - 'zagreb'
jobs:
  push_to_registry:
    name: Build and push Docker image to registry
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Scaleway
        uses: docker/login-action@v2
        with:
          registry: rg.fr-par.scw.cloud/djnd
          username: nologin
          password: ${{ secrets.SCW_SECRET_TOKEN }}

      - name: Build and push parladata
        uses: docker/build-push-action@v3
        with:
          context: ./parladata
          file: dockerfiles/parladata.Dockerfile
          push: true
          tags: |
            rg.fr-par.scw.cloud/djnd/parladata-zagreb:${{ github.sha }}
            rg.fr-par.scw.cloud/djnd/parladata-zagreb:latest

      - name: Build and push solr
        uses: docker/build-push-action@v3
        with:
          context: ./parladata/solr
          file: dockerfiles/solr.Dockerfile
          push: true
          tags: |
            rg.fr-par.scw.cloud/djnd/parlasolr-zagreb:${{ github.sha }}
            rg.fr-par.scw.cloud/djnd/parlasolr-zagreb:latest

      - name: Build and push parlassets
        uses: docker/build-push-action@v3
        with:
          context: .
          file: dockerfiles/parlafront.Dockerfile
          target: parlassets
          build-args: |
            VITE_PARLASSETS_URL=https://cdn.zagreb.parlametar.hr
          push: true
          tags: |
            rg.fr-par.scw.cloud/djnd/parlassets-zagreb:${{ github.sha }}
            rg.fr-par.scw.cloud/djnd/parlassets-zagreb:latest

      - name: Build and push parlacards
        uses: docker/build-push-action@v3
        with:
          context: .
          file: dockerfiles/parlafront.Dockerfile
          target: parlacards
          build-args: |
            VITE_PARLASSETS_URL=https://cdn.zagreb.parlametar.hr
          push: true
          tags: |
            rg.fr-par.scw.cloud/djnd/parlacards-zagreb:${{ github.sha }}
            rg.fr-par.scw.cloud/djnd/parlacards-zagreb:latest

      - name: Build and push parlasite
        uses: docker/build-push-action@v3
        with:
          context: .
          file: dockerfiles/parlasite.Dockerfile
          push: true
          tags: |
            rg.fr-par.scw.cloud/djnd/parlasite-zagreb:${{ github.sha }}
            rg.fr-par.scw.cloud/djnd/parlasite-zagreb:latest

      - name: Update image versions in kustomize/kustomization.yaml
        run: |
          sed -i "s/newTag: .*\+/newTag\: '${{ github.sha }}'/" kustomize/kustomization.yaml

      - name: Commit and push new image versions
        uses: EndBug/add-and-commit@v9
        with:
          add: '.'
          message: 'Update image versions in kustomize/kustomization.yaml'
